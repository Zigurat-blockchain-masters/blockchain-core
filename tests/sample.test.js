describe('add function', () => {
    it('should add two numbers correctly', () => {
        const result = 2 + 3;
        expect(result).toBe(5); // Expect the result to be 5 when adding 2 and 3
    });
});